<div class="posts">
  {% for post in postslist | reverse %}
    <div class="post">
      {% if post.data.thumbnail %}
        <img src="{{post.data.thumbnail}}"/>
      {% endif %}

      <div>
        <time datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate("yyyy-MM-dd") }}</time><br/>

        <a href="{{ post.url }}">
          {% if post.data.title %}{{ post.data.title }}
          {% else %}
            <code>{{ post.url }}</code>
          {% endif %}
        </a>
      </div>
    </div>
  {% endfor %}
</div>
